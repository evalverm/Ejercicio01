import { ItemClassDirectiveDirective } from './item-class-directive.directive';

describe('ItemClassDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemClassDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
